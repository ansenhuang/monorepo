import React from 'react';

export interface SetterAreaProps {}

const SetterArea: React.FC<SetterAreaProps> = () => {
  return <div style={{ height: '100%', overflow: 'hidden' }}>组件配置项</div>;
};

export default SetterArea;
